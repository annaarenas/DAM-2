<Window x:Class="Ejemplo_ListBox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Ejemplo_ListBox"
        mc:Ignorable="d"
        Title="Ejemplo ListBox" Height="350" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="150"/>
        </Grid.RowDefinitions>

        <ListBox Name="listaPoblaciones" HorizontalContentAlignment="Stretch">
            
            <ListBox.ItemTemplate>
                
                <DataTemplate>
                    
                    <Grid Margin="2">

                        <Grid.ColumnDefinitions>

                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>

                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="{Binding Poblacion1}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Temp1}"/>
                        <TextBlock Grid.Column="2" Text="{Binding Poblacion2}"/>
                        <TextBlock Grid.Column="3" Text="{Binding Temp2}"/>

                    </Grid>
                    
                </DataTemplate>
                
            </ListBox.ItemTemplate>
            
        </ListBox>

        <Button Grid.Row="1" Content="Info" Margin="20" Click="Button_Click"/>

    </Grid>
</Window>
